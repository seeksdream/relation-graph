import React from 'react';

const RGSlotOnGraph: React.FC = ({ children }) => {
  // const graphInstance = useContext(RelationGraphStoreContext);
  // const options = graphInstance && graphInstance.options;
  // console.log('RGSlotOnGraph:xxxxxxxxxx2');
  return <>{children}</>;
};
export default RGSlotOnGraph;
